import React, { FunctionComponent } from 'react';

const TabContentHeader: FunctionComponent = ({ children }) => {
  return <div className="tabs__content__header">{children}</div>;
};

export { TabContentHeader };
