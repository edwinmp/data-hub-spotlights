import React, { FunctionComponent } from 'react';

const PageSectionHeading: FunctionComponent = ({ children }) => {
  return <h2 className="section__heading">{children}</h2>;
};

export { PageSectionHeading };
